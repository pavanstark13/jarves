# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.6.1

# Storage — SQLite by default; asyncpg is only needed to point at Postgres
sqlalchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0

# Market data and maths
httpx==0.27.2
numpy==2.1.3

# The agent's timer
apscheduler==3.10.4
python-dotenv==1.0.1

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
